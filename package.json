{
  "name": "test-monorepo-root-workspace-build",
  "version": "0.1.1",
  "private": true,
  "workspaces": [
    "web-app",
    "landing"
  ],
  "scripts": {
    "build": "npm run build --workspace=web-app && npm run build --workspace=landing",
    "start": "npm run start --workspace=web-app"
  }
}
